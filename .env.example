# GLAD Studio Environment Configuration
#
# The GLAD Studio website uses direct-to-browser integrations (e.g. FormSubmit AJAX)
# and Cal.com scheduling. No mandatory environment variables, API keys, or server secrets
# are required to run, build, or deploy the application.
